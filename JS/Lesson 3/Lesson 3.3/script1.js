let number = 4;
let margin = 5;
let result = (1024 - margin * 2 * number)/number;
alert(result)

let count =+ prompt("INPUT NUMBER");
let width =+ prompt("input width");
number = 1024/(width + 2 * margin);


let sum = count/number;
let mod = count-sum * number;

alert(`${Math.floor(number)} ${Math.floor(sum)} ${Math.floor(mod)}`); 










